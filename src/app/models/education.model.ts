export interface Department{
    id: number;
    name:string;
    departmentId:string;
    description:string;
  createdBy: string,
  createdDate: string,
  updatedDate: string,
  updatedBy: string,
  isActive: Boolean
}